# 3rdparty

该目录用于记录或放置无法通过系统包管理器、vcpkg、CPM.cmake 获取的第三方依赖。

当前第一阶段代码骨架不 vendoring 第三方源码，原因是：

- 保持仓库轻量。
- 默认构建不依赖 Qt、Open3D、PCL、ROS 或厂商 SDK。
- 后续依赖优先通过 vcpkg manifest、系统包或插件私有依赖接入。

建议规则：

- 能通过 vcpkg 管理的库，不直接放入本目录。
- 厂商 SDK 只作为插件私有依赖，不进入核心应用。
- 若必须放入源码或二进制包，需要在本目录补充许可证、来源、版本和更新方式。
